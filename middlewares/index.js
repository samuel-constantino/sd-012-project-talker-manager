const error = require('./error');

const emailValid = require('./emailValid');

const passwordValid = require('./passwordValid');

const tokenValid = require('./tokenValid');

const nameValid = require('./nameValid');

const ageValid = require('./ageValid');

const talkValid = require('./talkValid');

module.exports = {
    error,
    emailValid,
    passwordValid,
    tokenValid,
    nameValid,
    ageValid,
    talkValid,
};